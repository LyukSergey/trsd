spring:
  application:
    name: gz_32_starter_test
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: ${postgresql_user:sa}
    password: ${postgresql_password:sa}

profiling:
  enabled: true
